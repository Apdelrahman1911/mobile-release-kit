# Fixed Android preparation DATA

This is a finite sanitized projection of the already reviewed supplier/layout
records. It is not a package resolver, executable SDK, permission or native pass.
`policy.json` is pinned by `android_material_preparation.py`; it in turn binds
the exact stored and decoded bytes of the supplier, layout, archive, font and provider
controls.

- 146 previously acquired bodies plus the two genuine ncurses5 ABI packages.
- The existing252 POM/module URLs and one exact official SDK repository XML.
- 678,706,726 maximum supplier body bytes, before separately reserved outputs.
- 12,367 unchanged tool leaves, four explicit generated inputs, and three ABI5
  additions (two genuine libraries plus one copyright notice), plus the fixed
  AGP aapt2 member at `gradle/native/aapt2/aapt2`. The 12,371 nongenerated leaves
  total 993,914,038 bytes, before generated inputs and the full OS union.
- Archive alias meanings are already resolved by the accepted layout. Staging
  writes only fresh regular leaves; it never installs archive aliases/scripts.

`sourceCommitments` keeps the original acquisition, complete layout, metadata,
ABI and normalization evidence commitments without host identities or private
paths. The compressed controls contain only finite filenames, supplier URLs,
sizes, hashes, modes, archive metadata and exact member-to-destination routes.
No SDK/JDK/Maven bodies, configuration, CA store or licence text is checked in.

`fonts.json` binds the fixed 53 font files, 35 selected configuration files,
their exact directory memberships, and the two fontconfig consumers with their
11-provider closure. It contains only portable supplier projections, not cache
identities or cache digests from another VM. The existing command owner pairs
bounded `fc-cat` cache membership with `fc-list` full paths on the same VM.
This establishes consumer correspondence, not cache-generation provenance;
both original inputs and captured outputs must remain unchanged on readback.

`providers.json` binds 87 actual tool ELF records, six nested GNU/Linux JNI
members, 40 OS library suppliers and eight helper/font consumers. The graph is
per launch and load phase: inherited RPATH, direct RUNPATH and loaded SONAMEs
are not a global provider dictionary. The GNU post-JLI global JVM phase does
not authorize unrelated SDK launches. The SDK-private ABI5 pair cannot satisfy
the JNI global ncurses5 request; its expected miss precedes the separate
ncurses6 fallback structure. This is version-node/dependency DATA, not symbol
binding, selected-native-member consumption or execution evidence.

The two fixed loader DATA consumers reuse the original compiler command owner;
their bounded private captures, cache/configuration originals and all finite
cache/default/hwcaps alternatives are rechecked before and after use. Readback
does not execute commands. The complete tool+generated+OS union, documents and
descriptor roster are charged before creation/download, without deletion credit.
The fixed aapt2 leaf is a second route from body-010's existing archive/member
owner, not another acquisition, SDK aapt2 or a guessed Gradle extraction path.
The selected AGP launch override and fixed JNA private-work extraction arguments
must accompany this new source/material tuple; an older profile cannot run it.

The two package.xml hashes are the deterministic projection of the pinned
official package definitions (API35 revision2; build-tools35.0.0), with their
original type-details, licence, revision and display-name. They are not files
claimed to have been inside the supplier ZIP. Repository31.9.2's actual
TrimStringAdapter/License classes establish the licence normalization; the
pre-existing receipt must contain24333f8a63b6825ea9c5514f83c2829b004d1fee. The
adapter preserves actual pre-existing receipt bytes; it never accepts terms.

## Remaining target fact gate

`hostPolicy` is deliberately null. The existing host-material route still must
establish the complete target supplier/version/helper/font/configuration/alias
closure, Java CA producer/input provenance, resolver/configuration boundaries,
and the origin of the fixed hosted-image SDK receipt. Jansi/fileevents selected
origins, ncurses fallback and same-VM native member/helper settlement still need
their applicable source/native evidence. Portable supplier facts
must not be confused with same-VM generated observations. No unreviewed host
snapshot or VPS hash is promoted merely by supplying a hash-shaped value.

The helper refuses before acquisition/creation while this policy is absent.
The Rust default remains an absent Android profile; both output executables
consume the same explicit, bounded OS input only after admitted preparation.
Ordinary Android qualification flags remain false.
