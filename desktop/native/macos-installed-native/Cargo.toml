[package]
name = "mrk-macos-installed-native"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
license = "MIT"
publish = false

[features]
default = []
# Instrumented installed-app qualification only; never a shipping control.
installed-observation = []

[build-dependencies]
cc = "=1.4.6"
