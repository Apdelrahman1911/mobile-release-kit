{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the seven passive application commands, including pure draft suggestion and preview. No shell, filesystem, opener, or remote authority.",
  "windows": ["main"],
  "local": true,
  "permissions": ["allow-app-info", "allow-choose-project", "allow-project-snapshot", "allow-catalog", "allow-validate-config", "allow-suggest-config", "allow-preview-config"]
}
