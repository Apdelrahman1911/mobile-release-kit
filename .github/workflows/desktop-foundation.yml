name: Desktop foundation verification

on:
  push:
    branches: [feature/desktop-application, verify/desktop-windows-snapshot, verify/desktop-windows-installed-native, verify/desktop-conventional-prepare, verify/desktop-conventional-smoke]
    paths:
      - desktop/**
      - tests/desktop/**
      - tests/native_desktop_config.py
      - tests/native_desktop_config_eof.py
      - tests/native_desktop_snapshot_windows.py
      - src/mobile_release/api/**
      - src/mobile_release/_desktop_engine.py
      - src/mobile_release/_desktop_edit_*.py
      - src/mobile_release/config_edit.py
      - src/mobile_release/config_payloads.py
      - src/mobile_release/workflow_payloads.py
      - src/mobile_release/cli.py
      - src/mobile_release/init_workspace_custody.py
      - src/mobile_release/cancellation.py
      - src/mobile_release/build_inputs.py
      - src/mobile_release/__init__.py
      - src/mobile_release/config.py
      - src/mobile_release/discovery.py
      - src/mobile_release/credential_requirements.py
      - src/mobile_release/credential_policy.py
      - src/mobile_release/errors.py
      - src/mobile_release/init_transaction.py
      - src/mobile_release/metadata.py
      - src/mobile_release/reporting.py
      - pyproject.toml
      - .github/workflows/desktop-foundation.yml
  workflow_dispatch:
    inputs:
      scope:
        description: Fixed verification scope (no release or Store operation)
        required: true
        type: choice
        default: foundation
        options: [foundation, windows-snapshot, windows-installed-native, windows-installed-fullwalk, windows-runtime-publication, conventional-prepare, conventional-smoke]
      expected_sha:
        description: Exact reviewed commit on the selected ref (verification only)
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: desktop-foundation-${{ github.ref }}
  cancel-in-progress: false

jobs:
  passive-native:
    name: Desktop boundary / ${{ matrix.platform }}
    if: >-
      (github.event_name == 'push' && github.ref == 'refs/heads/feature/desktop-application') ||
      (github.event_name == 'workflow_dispatch' && inputs.scope == 'foundation' &&
       github.ref != 'refs/heads/verify/desktop-conventional-prepare' &&
       github.ref != 'refs/heads/verify/desktop-conventional-smoke' &&
       github.ref != 'refs/heads/verify/desktop-windows-installed-native' &&
       github.ref != 'refs/heads/verify/desktop-windows-runtime-publication')
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - platform: linux
            os: ubuntu-24.04
          - platform: macos
            os: macos-26
          - platform: windows
            os: windows-2025
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    env:
      MRK_DESKTOP_PLATFORM: ${{ matrix.platform }}
      MRK_DESKTOP_HOSTED_CHECKS: passive-v1
      MRK_DESKTOP_DISPATCH_SCOPE: ${{ inputs.scope }}
      MRK_DESKTOP_EXPECTED_SHA: ${{ inputs.expected_sha }}
      NODE_DISABLE_COMPILE_CACHE: '1'
    steps:
      - name: Require disposable hosting and exact-source selection
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          EXPECTED_SHA: ${{ inputs.expected_sha }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          set -euo pipefail
          [[ "$RUNNER_ENVIRONMENT" == github-hosted ]]
          [[ "$GITHUB_SHA" =~ ^[0-9a-f]{40}$ ]]
          if [[ "$EVENT_NAME" == workflow_dispatch ]]; then
            [[ "$EXPECTED_SHA" =~ ^[0-9a-f]{40}$ && "$EXPECTED_SHA" == "$GITHUB_SHA" ]]
          else
            [[ "$EVENT_NAME" == push && "$GITHUB_REF" == refs/heads/feature/desktop-application ]]
          fi
      - name: Check out exact candidate without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        env:
          # Check out exact blob line endings even on Windows. Later source
          # checks intentionally ignore the image's system/global Git config.
          GIT_CONFIG_COUNT: '1'
          GIT_CONFIG_KEY_0: core.autocrlf
          GIT_CONFIG_VALUE_0: 'false'
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Select fixed native Python
        id: python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.14.7'
      - name: Select fixed frontend compiler
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: '24.20.0'
          package-manager-cache: false
      - name: Install Linux native shell compilation prerequisites
        if: matrix.platform == 'linux'
        timeout-minutes: 8
        shell: bash
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends libgtk-3-dev libwebkit2gtk-4.1-dev librsvg2-dev
          dpkg-query -W -f='${Package} ${Version}\n' libgtk-3-dev libwebkit2gtk-4.1-dev librsvg2-dev
      - name: Prepare bounded synthetic inputs and source bindings
        id: prepare
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py prepare'
      - name: Acquire only locked active-platform compiler inputs
        timeout-minutes: 15
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py acquire'
      - name: Compile headless tests and applicable native shell
        timeout-minutes: 25
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py compile'
      - name: Verify genuine passive IPC and original-child settlement
        timeout-minutes: 5
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py native'
      - name: Verify configuration IPC and original-resource settlement
        if: matrix.platform != 'windows'
        timeout-minutes: 5
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py config-owner'
      - name: Verify original native cleanup after controlled management-task failure
        if: matrix.platform != 'windows'
        timeout-minutes: 5
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py config-task-loss'
      - name: Verify original EOF cancellation and nonrenewable lifecycle deadlines
        if: matrix.platform != 'windows'
        timeout-minutes: 5
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py config-owner-delta'
      - name: Verify real EOF before and after durable configuration commit
        if: matrix.platform != 'windows'
        timeout-minutes: 3
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py config-transaction-eof'
      - name: Verify core transactions before the last retained terminal-fault control
        if: matrix.platform != 'windows'
        timeout-minutes: 5
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py config-core'
      - name: Retain only allowlisted synthetic evidence
        if: always() && steps.prepare.outcome == 'success'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: desktop-foundation-${{ matrix.platform }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            ${{ steps.prepare.outputs.root }}/public-bindings.json
            ${{ steps.prepare.outputs.root }}/acquire-checks.json
            ${{ steps.prepare.outputs.root }}/compile-checks.json
            ${{ steps.prepare.outputs.root }}/native-checks.json
            ${{ steps.prepare.outputs.root }}/native/receipt.json
            ${{ steps.prepare.outputs.root }}/config-owner-checks.json
            ${{ steps.prepare.outputs.root }}/config-owner/receipt.json
            ${{ steps.prepare.outputs.root }}/config-task-loss-checks.json
            ${{ steps.prepare.outputs.root }}/config-driver-loss/receipt.json
            ${{ steps.prepare.outputs.root }}/config-watchdog-loss/receipt.json
            ${{ steps.prepare.outputs.root }}/config-owner-delta-checks.json
            ${{ steps.prepare.outputs.root }}/config-stop/receipt.json
            ${{ steps.prepare.outputs.root }}/config-terminal-deadline/receipt.json
            ${{ steps.prepare.outputs.root }}/config-startup-stop/receipt.json
            ${{ steps.prepare.outputs.root }}/config-transaction-eof-checks.json
            ${{ steps.prepare.outputs.root }}/config-transaction-eof/receipt.json
            ${{ steps.prepare.outputs.root }}/config-core-checks.json
            ${{ steps.prepare.outputs.root }}/config-ordinary.json
            ${{ steps.prepare.outputs.root }}/config-committed-fsync.json
            ${{ steps.prepare.outputs.root }}/config-committed-close.json
          if-no-files-found: error
          retention-days: 14
          compression-level: 1
      - name: Remove settled task-owned disposable outputs
        if: success()
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py clean'

  windows-snapshot:
    name: Windows static snapshot / original native boundaries
    if: >-
      (github.event_name == 'push' && github.ref == 'refs/heads/verify/desktop-windows-snapshot') ||
      (github.event_name == 'workflow_dispatch' && inputs.scope == 'windows-snapshot' &&
       github.ref != 'refs/heads/verify/desktop-conventional-prepare' &&
       github.ref != 'refs/heads/verify/desktop-conventional-smoke' &&
       github.ref != 'refs/heads/verify/desktop-windows-installed-native' &&
       github.ref != 'refs/heads/verify/desktop-windows-runtime-publication')
    runs-on: windows-2025
    timeout-minutes: 45
    env:
      MRK_DESKTOP_PLATFORM: windows
      MRK_DESKTOP_HOSTED_CHECKS: windows-snapshot-v1
      MRK_DESKTOP_DISPATCH_SCOPE: ${{ inputs.scope }}
      MRK_DESKTOP_EXPECTED_SHA: ${{ inputs.expected_sha }}
    steps:
      - name: Require the reviewed Windows-only source and disposable host
        shell: bash
        env:
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          set -euo pipefail
          [[ "$RUNNER_ENVIRONMENT" == github-hosted && "$RUNNER_OS" == Windows && "$RUNNER_ARCH" == X64 ]]
          [[ "$GITHUB_SHA" =~ ^[0-9a-f]{40}$ ]]
          if [[ "$GITHUB_EVENT_NAME" == workflow_dispatch ]]; then
            [[ "$MRK_DESKTOP_DISPATCH_SCOPE" == windows-snapshot ]]
            [[ "$MRK_DESKTOP_EXPECTED_SHA" =~ ^[0-9a-f]{40}$ && "$MRK_DESKTOP_EXPECTED_SHA" == "$GITHUB_SHA" ]]
          else
            [[ "$GITHUB_EVENT_NAME" == push && "$GITHUB_REF" == refs/heads/verify/desktop-windows-snapshot ]]
          fi
      - name: Check out exact candidate without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        env:
          GIT_CONFIG_COUNT: '1'
          GIT_CONFIG_KEY_0: core.autocrlf
          GIT_CONFIG_VALUE_0: 'false'
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Select fixed native Python
        id: python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.14.7'
      - name: Prepare bounded synthetic Windows inputs and source bindings
        id: prepare
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py prepare'
      - name: Acquire only locked headless Windows compiler inputs
        timeout-minutes: 15
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py acquire'
      - name: Compile only the original-owner headless tests
        timeout-minutes: 15
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py compile'
      - name: Verify the fixed Windows snapshot controls with original ownership
        timeout-minutes: 6
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py windows-snapshot'
      - name: Retain only bounded synthetic Windows evidence
        if: always() && steps.prepare.outcome == 'success'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: desktop-windows-snapshot-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            ${{ steps.prepare.outputs.root }}/public-bindings.json
            ${{ steps.prepare.outputs.root }}/acquire-checks.json
            ${{ steps.prepare.outputs.root }}/compile-checks.json
            ${{ steps.prepare.outputs.root }}/windows-snapshot-checks.json
            ${{ steps.prepare.outputs.root }}/windows-snapshot/receipt.json
          if-no-files-found: error
          retention-days: 14
          compression-level: 1
      - name: Remove only proven-settled task-owned disposable outputs
        if: success()
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py clean'

  conventional-prepare:
    name: Conventional accepted H to prepared DATA only
    if: >-
      github.ref == 'refs/heads/verify/desktop-conventional-prepare' &&
      (github.event_name == 'push' ||
       (github.event_name == 'workflow_dispatch' && inputs.scope == 'conventional-prepare'))
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read
      actions: read
    env:
      MRK_DESKTOP_HOSTED_CHECKS: conventional-runtime-data-preparation-v1
      MRK_DESKTOP_DISPATCH_SCOPE: ${{ inputs.scope }}
      MRK_DESKTOP_EXPECTED_SHA: ${{ inputs.expected_sha }}
      MRK_PUSH_EVENT_AFTER: ${{ github.event.after }}
    steps:
      - name: Check out reviewed preparation source without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Require source-literal H COPY committed review DATA and hosted TCB admissions
        id: admit
        shell: bash
        env:
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: /usr/bin/python3.12 -I -S -B desktop/tools/ci_foundation.py conventional-admit
      # These are transport outputs of the reviewed literal gate, not workflow
      # inputs or freshly observed hash authority. The token exists only here.
      - name: Download exactly the independently accepted original H artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          repository: ${{ steps.admit.outputs.source-repository }}
          run-id: ${{ steps.admit.outputs.source-runId }}
          artifact-ids: ${{ steps.admit.outputs.source-artifactId }}
          merge-multiple: true
          github-token: ${{ github.token }}
          digest-mismatch: error
          path: ${{ steps.admit.outputs.root }}/source-artifact
      - name: Copy and prepare once as DATA with original shell wait and writer closes
        id: prepare
        timeout-minutes: 10
        shell: bash
        env:
          CONVENTIONAL_ROOT: ${{ steps.admit.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          set -euo pipefail
          set -C
          umask 077
          exec 4>"$CONVENTIONAL_ROOT/prepare.stdout"
          exec 5>"$CONVENTIONAL_ROOT/prepare.stderr"
          set +e
          /usr/bin/python3.12 -I -S -B desktop/tools/ci_foundation.py conventional-prepare </dev/null 1>&4 2>&5 4>&- 5>&-
          result=$?
          set -e
          exec 4>&- 5>&- || exit 125
          (( result == 0 )) || exit "$result"
          exec 3>"$CONVENTIONAL_ROOT/public/outer.json"
          printf '{"schemaVersion":1,"scope":"conventional-runtime-data-preparation-v1","repository":"%s","sourceSha":"%s","runId":"%s","attempt":%s,"originalWait":true,"exitCode":%s,"outputWritersClosed":true,"statusWriterCloseGate":"original-step-success-required"}\n' "$GITHUB_REPOSITORY" "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" "$result" >&3 || exit 125
          exec 3>&- || exit 125
      - name: Retain closed prepared archive and approved public source and notices
        if: success() && steps.prepare.outcome == 'success'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: desktop-conventional-prepared-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ steps.admit.outputs.root }}/public
          if-no-files-found: error
          retention-days: 14
          compression-level: 0
    # NO candidate, Cargo, probe, libtest, cleanup or automatic smoke dependency.
    # Actual A artifact/tar/M/Q/source delivery needs independent acceptance and
    # a later literal-pin commit before a separate B push/dispatch is admitted.

  conventional-smoke:
    name: Conventional independently pinned W smoke only
    if: >-
      github.ref == 'refs/heads/verify/desktop-conventional-smoke' &&
      (github.event_name == 'push' ||
       (github.event_name == 'workflow_dispatch' && inputs.scope == 'conventional-smoke'))
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    permissions:
      contents: read
      actions: read
    env:
      MRK_DESKTOP_HOSTED_CHECKS: conventional-runtime-bootstrap-smoke-v1
      MRK_DESKTOP_DISPATCH_SCOPE: ${{ inputs.scope }}
      MRK_DESKTOP_EXPECTED_SHA: ${{ inputs.expected_sha }}
      MRK_PUSH_EVENT_AFTER: ${{ github.event.after }}
    steps:
      - name: Check out reviewed consumer source without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Require independent prepared artifact M Q and hosted DATA TCB pins
        id: admit
        shell: bash
        env:
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: /usr/bin/python3.12 -I -S -B desktop/tools/ci_foundation.py conventional-admit
      - name: Download exactly the independently accepted preparation artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          repository: ${{ steps.admit.outputs.prepared-repository }}
          run-id: ${{ steps.admit.outputs.prepared-runId }}
          artifact-ids: ${{ steps.admit.outputs.prepared-artifactId }}
          merge-multiple: true
          github-token: ${{ github.token }}
          digest-mismatch: error
          path: ${{ steps.admit.outputs.root }}/prepared-artifact
      - name: Authenticate unpack and compile the two fixed headless profiles
        timeout-minutes: 25
        shell: bash
        env:
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: /usr/bin/python3.12 -I -S -B desktop/tools/ci_foundation.py conventional-compile
      - name: Require closed owner refusal then run the fixed probe and two ZIP cases
        id: smoke
        timeout-minutes: 5
        shell: bash
        env:
          CONVENTIONAL_ROOT: ${{ steps.admit.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          set -euo pipefail
          set +o posix
          unset POSIXLY_CORRECT
          set -C
          umask 077
          # Original owner byte capture is separately capped at 2 MiB. Compile
          # and archive extraction already finished; this is only the smoke.
          ulimit -S -f 2048
          exec 4>"$CONVENTIONAL_ROOT/smoke.stdout"
          exec 5>"$CONVENTIONAL_ROOT/smoke.stderr"
          set +e
          /usr/bin/python3.12 -I -S -B desktop/tools/ci_foundation.py conventional-smoke </dev/null 1>&4 2>&5 4>&- 5>&-
          result=$?
          set -e
          exec 4>&- 5>&- || exit 125
          (( result == 0 )) || exit "$result"
          exec 3>"$CONVENTIONAL_ROOT/public/outer.json"
          printf '{"schemaVersion":1,"scope":"conventional-runtime-bootstrap-smoke-v1","repository":"%s","sourceSha":"%s","runId":"%s","attempt":%s,"originalWait":true,"exitCode":%s,"outputWritersClosed":true,"statusWriterCloseGate":"original-step-success-required"}\n' "$GITHUB_REPOSITORY" "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" "$result" >&3 || exit 125
          exec 3>&- || exit 125
      - name: Retain bounded original smoke receipts and approved source and notices
        if: success() && steps.smoke.outcome == 'success'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: desktop-conventional-smoke-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            ${{ steps.admit.outputs.root }}/public
            ${{ steps.admit.outputs.root }}/prepared-artifact/source-kit/hosted-evidence.tar
            ${{ steps.admit.outputs.root }}/prepared-artifact/source-kit/retained-files.json
            ${{ steps.admit.outputs.root }}/prepared-artifact/source-kit/hosted-summary.json
            ${{ steps.admit.outputs.root }}/prepared-artifact/components.json
            ${{ steps.admit.outputs.root }}/prepared-artifact/notice-inventory.json
            ${{ steps.admit.outputs.root }}/prepared-artifact/notices
            ${{ steps.admit.outputs.root }}/prepared-artifact/reviews
          if-no-files-found: error
          retention-days: 14
          compression-level: 0
    # No retry, fresh-root reset, cleanup or wider foundation/TLS/native jobs.


  windows-installed-native:
    name: Windows MSVC headless reader and native facts / no runtime enablement
    if: >-
      (github.ref == 'refs/heads/verify/desktop-windows-installed-native' &&
       (github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && (inputs.scope == 'windows-installed-native' || inputs.scope == 'windows-installed-fullwalk')))) ||
      (github.ref == 'refs/heads/verify/desktop-windows-runtime-publication' &&
       github.event_name == 'workflow_dispatch' && inputs.scope == 'windows-runtime-publication')
    runs-on: windows-2025-vs2026
    timeout-minutes: 35
    env:
      MRK_DESKTOP_PLATFORM: windows
      MRK_DESKTOP_HOSTED_CHECKS: windows-installed-native-v1
      MRK_DESKTOP_DISPATCH_SCOPE: ${{ inputs.scope }}
      MRK_DESKTOP_EXPECTED_SHA: ${{ inputs.expected_sha }}
    steps:
      - name: Require the fixed disposable Windows source envelope
        shell: bash
        env:
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          set -euo pipefail
          [[ "${RUNNER_ENVIRONMENT:-}" == github-hosted && "${RUNNER_OS:-}" == Windows && "${RUNNER_ARCH:-}" == X64 ]] || {
            printf '%s\n' 'MRK_WINDOWS_INSTALLED_ROUTE_REFUSED=runner' >&2; exit 1;
          }
          [[ "${ImageOS:-}" == win25-vs2026 ]] || {
            printf '%s\n' 'MRK_WINDOWS_INSTALLED_ROUTE_REFUSED=image' >&2; exit 1;
          }
          [[ "${GITHUB_RUN_ATTEMPT:-}" == 1 ]] || {
            printf '%s\n' 'MRK_WINDOWS_INSTALLED_ROUTE_REFUSED=attempt' >&2; exit 1;
          }
          [[ "${GITHUB_SHA:-}" =~ ^[0-9a-f]{40}$ && "${GITHUB_WORKFLOW_SHA:-}" == "${GITHUB_SHA:-}" ]] || {
            printf '%s\n' 'MRK_WINDOWS_INSTALLED_ROUTE_REFUSED=source' >&2; exit 1;
          }
          if [[ "${GITHUB_EVENT_NAME:-}" == workflow_dispatch ]]; then
            [[ "${MRK_DESKTOP_EXPECTED_SHA:-}" == "${GITHUB_SHA:-}" &&
               ( ( "${GITHUB_REF:-}" == refs/heads/verify/desktop-windows-installed-native &&
                   ( "${MRK_DESKTOP_DISPATCH_SCOPE:-}" == windows-installed-native || "${MRK_DESKTOP_DISPATCH_SCOPE:-}" == windows-installed-fullwalk ) ) ||
                 ( "${GITHUB_REF:-}" == refs/heads/verify/desktop-windows-runtime-publication &&
                   "${MRK_DESKTOP_DISPATCH_SCOPE:-}" == windows-runtime-publication ) ) ]] || {
              printf '%s\n' 'MRK_WINDOWS_INSTALLED_ROUTE_REFUSED=dispatch' >&2; exit 1;
            }
          else
            [[ "${GITHUB_EVENT_NAME:-}" == push && "${GITHUB_REF:-}" == refs/heads/verify/desktop-windows-installed-native &&
               -z "${MRK_DESKTOP_DISPATCH_SCOPE:-}" && -z "${MRK_DESKTOP_EXPECTED_SHA:-}" ]] || {
              printf '%s\n' 'MRK_WINDOWS_INSTALLED_ROUTE_REFUSED=event' >&2; exit 1;
            }
          fi
      - name: Check out exact candidate without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        env:
          GIT_CONFIG_COUNT: '1'
          GIT_CONFIG_KEY_0: core.autocrlf
          GIT_CONFIG_VALUE_0: 'false'
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Select fixed native Python
        id: python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.14.7'
          architecture: x64
      - name: Bind complete source and one original task root
        id: prepare
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py prepare'
      - name: Acquire only the locked headless app and native MSVC graphs
        timeout-minutes: 15
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py acquire'
      - name: Compile exact original libtests and the production-profile-only fixed helper
        timeout-minutes: 12
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py compile'
      - name: Preflight source-derived reader and producer policy selectors; do not launch ordinary work
        id: ordinary-preflight
        timeout-minutes: 4
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py windows-installed-native'
      - name: Publish one protected synthetic version and observe occupied-create refusal
        id: fixture-publisher
        if: success() && inputs.scope == 'windows-installed-fullwalk' && steps.ordinary-preflight.outcome == 'success'
        timeout-minutes: 4
        shell: pwsh -NoLogo -NoProfile -NonInteractive -Command ". '{0}'"
        env:
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_NATIVE_ARTIFACT: ${{ steps.ordinary-preflight.outputs.artifact }}
          MRK_WINDOWS_NATIVE_ARTIFACT_SHA256: ${{ steps.ordinary-preflight.outputs.artifactSha256 }}
          MRK_WINDOWS_SOURCE_TREE: ${{ steps.ordinary-preflight.outputs.sourceTree }}
          MRK_WINDOWS_PRECHECK_SHA256: ${{ steps.ordinary-preflight.outputs.precheckSha256 }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $false
          try {
            $expectedRoot = [System.IO.Path]::Combine($env:RUNNER_TEMP, "mrk-windows-installed-native-$($env:GITHUB_RUN_ID)-1")
            $expectedDeps = [System.IO.Path]::Combine($expectedRoot, 'target', 'x86_64-pc-windows-msvc', 'debug', 'deps')
            if ($env:GITHUB_RUN_ATTEMPT -cne '1' -or $env:MRK_DESKTOP_CI_ROOT -cne $expectedRoot -or
                $env:GITHUB_EVENT_NAME -cne 'workflow_dispatch' -or $env:MRK_DESKTOP_DISPATCH_SCOPE -cne 'windows-installed-fullwalk' -or
                $env:MRK_DESKTOP_EXPECTED_SHA -cne $env:GITHUB_SHA -or
                [System.IO.Path]::GetDirectoryName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cne $expectedDeps -or
                [System.IO.Path]::GetFileName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cnotmatch '^mrk_windows_installed_native-[0-9a-f]{16}\.exe$' -or
                $env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_SOURCE_TREE -cnotmatch '^[0-9a-f]{40}$') { throw 'binding' }
            if ($env:MRK_WINDOWS_PRECHECK_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or $env:MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME -cne 'success') { throw 'precheck' }
            Set-Location -LiteralPath $expectedRoot
            # One fixed direct foreground original. No Process object, pipe, retry or timeout-kill wrapper.
            & $env:MRK_WINDOWS_NATIVE_ARTIFACT qualification_fixture::hosted_publish_protected_version_fixture --exact --ignored --nocapture --test-threads=1
            $originalExitCode = $LASTEXITCODE
            if ($originalExitCode -isnot [int]) { throw 'original-exit' }
            $receipt = [ordered]@{
              schemaVersion = 1; sourceSha = $env:GITHUB_SHA; sourceTree = $env:MRK_WINDOWS_SOURCE_TREE
              runId = $env:GITHUB_RUN_ID; attempt = 1
              artifactSha256 = $env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256
              publisherTest = 'qualification_fixture::hosted_publish_protected_version_fixture'
              precheckSha256 = $env:MRK_WINDOWS_PRECHECK_SHA256
              originalWaitReturned = $true; exitCode = $originalExitCode
              writerCloseGate = 'original-publisher-step-success-required'
            }
            $raw = [System.Text.UTF8Encoding]::new($false, $true).GetBytes(($receipt | ConvertTo-Json -Depth 4 -Compress))
            if ($raw.Length -eq 0 -or $raw.Length -gt 4096) { throw 'exit-bound' }
            $path = [System.IO.Path]::Combine($expectedRoot, 'fullwalk-publisher-exit.private.json')
            $stream = [System.IO.File]::Open($path, [System.IO.FileMode]::CreateNew, [System.IO.FileAccess]::Write, [System.IO.FileShare]::None)
            try { $stream.Write($raw, 0, $raw.Length); $stream.Flush() } finally { $stream.Dispose() }
            # Only THIS later original step success closes the preceding exit record.
            if ($originalExitCode -ne 0) { exit $originalExitCode }
          } catch {
            [Console]::Error.WriteLine('MRK_WINDOWS_PUBLISHER_REFUSED=original-or-exit-record')
            exit 1
          }
      - name: Stage only the fixed private runtime input with original native custody
        id: producer-stage
        if: success() && inputs.scope == 'windows-runtime-publication' && steps.ordinary-preflight.outcome == 'success'
        timeout-minutes: 4
        shell: pwsh -NoLogo -NoProfile -NonInteractive -Command ". '{0}'"
        env:
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_NATIVE_ARTIFACT: ${{ steps.ordinary-preflight.outputs.artifact }}
          MRK_WINDOWS_NATIVE_ARTIFACT_SHA256: ${{ steps.ordinary-preflight.outputs.artifactSha256 }}
          MRK_WINDOWS_SOURCE_TREE: ${{ steps.ordinary-preflight.outputs.sourceTree }}
          MRK_WINDOWS_PRECHECK_SHA256: ${{ steps.ordinary-preflight.outputs.precheckSha256 }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $false
          try {
            $expectedRoot = [System.IO.Path]::Combine($env:RUNNER_TEMP, "mrk-windows-installed-native-$($env:GITHUB_RUN_ID)-1")
            if ($env:GITHUB_RUN_ATTEMPT -cne '1' -or $env:MRK_DESKTOP_CI_ROOT -cne $expectedRoot -or
                $env:GITHUB_EVENT_NAME -cne 'workflow_dispatch' -or $env:MRK_DESKTOP_DISPATCH_SCOPE -cne 'windows-runtime-publication' -or
                $env:GITHUB_REF -cne 'refs/heads/verify/desktop-windows-runtime-publication' -or
                $env:MRK_DESKTOP_EXPECTED_SHA -cne $env:GITHUB_SHA -or $env:GITHUB_WORKFLOW_SHA -cne $env:GITHUB_SHA -or
                $env:GITHUB_SHA -cnotmatch '^[0-9a-f]{40}$' -or $env:MRK_WINDOWS_SOURCE_TREE -cnotmatch '^[0-9a-f]{40}$' -or
                $env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_PRECHECK_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME -cne 'success') { throw 'binding' }
            $expectedDeps = [System.IO.Path]::Combine($expectedRoot, 'target', 'x86_64-pc-windows-msvc', 'debug', 'deps')
            if ([System.IO.Path]::GetDirectoryName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cne $expectedDeps -or
                [System.IO.Path]::GetFileName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cnotmatch '^mrk_windows_installed_native-[0-9a-f]{16}\.exe$') { throw 'artifact' }
            Set-Location -LiteralPath $expectedRoot
            # One fixed direct foreground original, never a Process object, launcher, pipe, retry or kill.
            & $env:MRK_WINDOWS_NATIVE_ARTIFACT qualification_fixture::hosted_stage_fixed_runtime_input --exact --ignored --nocapture --test-threads=1
            $originalExitCode = $LASTEXITCODE
            if ($originalExitCode -isnot [int]) { throw 'original-exit' }
            $command = '"' + $env:MRK_WINDOWS_NATIVE_ARTIFACT + '" qualification_fixture::hosted_stage_fixed_runtime_input --exact --ignored --nocapture --test-threads=1'
            $commandSha256 = [System.Convert]::ToHexString([System.Security.Cryptography.SHA256]::HashData([System.Text.Encoding]::Unicode.GetBytes($command))).ToLowerInvariant()
            $lines = @(
              'MRK_WINDOWS_RUNTIME_PUBLICATION_ORIGINAL_EXIT_V1'
              'profile=windows-runtime-publication-v1'
              "sourceSha=$($env:GITHUB_SHA)"
              "sourceTree=$($env:MRK_WINDOWS_SOURCE_TREE)"
              "runId=$($env:GITHUB_RUN_ID)"
              'attempt=1'
              'role=stage'
              "artifactSha256=$($env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256)"
              "precheckSha256=$($env:MRK_WINDOWS_PRECHECK_SHA256)"
              "commandSha256=$commandSha256"
              'originalWaitReturned=true'
              "exitCode=$originalExitCode"
              'writerCloseGate=original-stage-step-success-required'
            )
            $raw = [System.Text.UTF8Encoding]::new($false, $true).GetBytes(($lines -join "`n") + "`n")
            if ($raw.Length -eq 0 -or $raw.Length -gt 4096) { throw 'exit-bound' }
            $path = [System.IO.Path]::Combine($expectedRoot, 'producer-stage-exit.private.txt')
            $stream = [System.IO.File]::Open($path, [System.IO.FileMode]::CreateNew, [System.IO.FileAccess]::Write, [System.IO.FileShare]::None)
            try { $stream.Write($raw, 0, $raw.Length); $stream.Flush() } finally { $stream.Dispose() }
            # Only THIS later original step success closes the preceding exit record.
            if ($originalExitCode -ne 0) { throw 'expected-original-exit' }
            exit 0
          } catch {
            [Console]::Error.WriteLine('MRK_WINDOWS_PRODUCER_REFUSED=stage-original-or-exit-record')
            exit 1
          }
      - name: Validate separately closed stage evidence as DATA
        id: producer-stage-finalize
        if: success() && inputs.scope == 'windows-runtime-publication' && steps.producer-stage.outcome == 'success'
        timeout-minutes: 2
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py windows-runtime-publication-stage-finalize'
      - name: Run the original fixed production helper once and require actual exit zero
        id: producer-success
        if: success() && inputs.scope == 'windows-runtime-publication' && steps.producer-stage-finalize.outcome == 'success'
        timeout-minutes: 4
        shell: pwsh -NoLogo -NoProfile -NonInteractive -Command ". '{0}'"
        env:
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_HELPER_ARTIFACT: ${{ steps.ordinary-preflight.outputs.helperArtifact }}
          MRK_WINDOWS_HELPER_ARTIFACT_SHA256: ${{ steps.ordinary-preflight.outputs.helperArtifactSha256 }}
          MRK_WINDOWS_SOURCE_TREE: ${{ steps.ordinary-preflight.outputs.sourceTree }}
          MRK_WINDOWS_PRECHECK_SHA256: ${{ steps.ordinary-preflight.outputs.precheckSha256 }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $false
          try {
            $expectedRoot = [System.IO.Path]::Combine($env:RUNNER_TEMP, "mrk-windows-installed-native-$($env:GITHUB_RUN_ID)-1")
            if ($env:GITHUB_RUN_ATTEMPT -cne '1' -or $env:MRK_DESKTOP_CI_ROOT -cne $expectedRoot -or
                $env:GITHUB_EVENT_NAME -cne 'workflow_dispatch' -or $env:MRK_DESKTOP_DISPATCH_SCOPE -cne 'windows-runtime-publication' -or
                $env:GITHUB_REF -cne 'refs/heads/verify/desktop-windows-runtime-publication' -or
                $env:MRK_DESKTOP_EXPECTED_SHA -cne $env:GITHUB_SHA -or $env:GITHUB_WORKFLOW_SHA -cne $env:GITHUB_SHA -or
                $env:GITHUB_SHA -cnotmatch '^[0-9a-f]{40}$' -or $env:MRK_WINDOWS_SOURCE_TREE -cnotmatch '^[0-9a-f]{40}$' -or
                $env:MRK_WINDOWS_HELPER_ARTIFACT_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_PRECHECK_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME -cne 'success') { throw 'binding' }
            $expectedHelper = [System.IO.Path]::Combine($expectedRoot, 'mrk-windows-runtime-publish.exe')
            if ($env:MRK_WINDOWS_HELPER_ARTIFACT -cne $expectedHelper) { throw 'artifact' }
            if ($env:MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME -cne 'success' -or
                $env:MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME -cne 'success') { throw 'prior-original-finality' }
            Set-Location -LiteralPath $expectedRoot
            # One fixed direct foreground original, never a Process object, launcher, pipe, retry or kill.
            & $env:MRK_WINDOWS_HELPER_ARTIFACT
            $originalExitCode = $LASTEXITCODE
            if ($originalExitCode -isnot [int]) { throw 'original-exit' }
            $command = '"' + $env:MRK_WINDOWS_HELPER_ARTIFACT + '"'
            $commandSha256 = [System.Convert]::ToHexString([System.Security.Cryptography.SHA256]::HashData([System.Text.Encoding]::Unicode.GetBytes($command))).ToLowerInvariant()
            $lines = @(
              'MRK_WINDOWS_RUNTIME_PUBLICATION_ORIGINAL_EXIT_V1'
              'profile=windows-runtime-publication-v1'
              "sourceSha=$($env:GITHUB_SHA)"
              "sourceTree=$($env:MRK_WINDOWS_SOURCE_TREE)"
              "runId=$($env:GITHUB_RUN_ID)"
              'attempt=1'
              'role=helperSuccess'
              "artifactSha256=$($env:MRK_WINDOWS_HELPER_ARTIFACT_SHA256)"
              "precheckSha256=$($env:MRK_WINDOWS_PRECHECK_SHA256)"
              "commandSha256=$commandSha256"
              'originalWaitReturned=true'
              "exitCode=$originalExitCode"
              'writerCloseGate=original-helperSuccess-step-success-required'
            )
            $raw = [System.Text.UTF8Encoding]::new($false, $true).GetBytes(($lines -join "`n") + "`n")
            if ($raw.Length -eq 0 -or $raw.Length -gt 4096) { throw 'exit-bound' }
            $path = [System.IO.Path]::Combine($expectedRoot, 'producer-success-exit.private.txt')
            $stream = [System.IO.File]::Open($path, [System.IO.FileMode]::CreateNew, [System.IO.FileAccess]::Write, [System.IO.FileShare]::None)
            try { $stream.Write($raw, 0, $raw.Length); $stream.Flush() } finally { $stream.Dispose() }
            # Only THIS later original step success closes the preceding exit record.
            if ($originalExitCode -ne 0) { throw 'expected-original-exit' }
            exit 0
          } catch {
            [Console]::Error.WriteLine('MRK_WINDOWS_PRODUCER_REFUSED=helperSuccess-original-or-exit-record')
            exit 1
          }
      - name: Validate separately closed helperSuccess evidence as DATA
        id: producer-success-finalize
        if: success() && inputs.scope == 'windows-runtime-publication' && steps.producer-success.outcome == 'success'
        timeout-minutes: 2
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py windows-runtime-publication-success-finalize'
      - name: Independently observe the produced version and unchanged staged source
        id: producer-before
        if: success() && inputs.scope == 'windows-runtime-publication' && steps.producer-success-finalize.outcome == 'success'
        timeout-minutes: 4
        shell: pwsh -NoLogo -NoProfile -NonInteractive -Command ". '{0}'"
        env:
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_NATIVE_ARTIFACT: ${{ steps.ordinary-preflight.outputs.artifact }}
          MRK_WINDOWS_NATIVE_ARTIFACT_SHA256: ${{ steps.ordinary-preflight.outputs.artifactSha256 }}
          MRK_WINDOWS_SOURCE_TREE: ${{ steps.ordinary-preflight.outputs.sourceTree }}
          MRK_WINDOWS_PRECHECK_SHA256: ${{ steps.ordinary-preflight.outputs.precheckSha256 }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $false
          try {
            $expectedRoot = [System.IO.Path]::Combine($env:RUNNER_TEMP, "mrk-windows-installed-native-$($env:GITHUB_RUN_ID)-1")
            if ($env:GITHUB_RUN_ATTEMPT -cne '1' -or $env:MRK_DESKTOP_CI_ROOT -cne $expectedRoot -or
                $env:GITHUB_EVENT_NAME -cne 'workflow_dispatch' -or $env:MRK_DESKTOP_DISPATCH_SCOPE -cne 'windows-runtime-publication' -or
                $env:GITHUB_REF -cne 'refs/heads/verify/desktop-windows-runtime-publication' -or
                $env:MRK_DESKTOP_EXPECTED_SHA -cne $env:GITHUB_SHA -or $env:GITHUB_WORKFLOW_SHA -cne $env:GITHUB_SHA -or
                $env:GITHUB_SHA -cnotmatch '^[0-9a-f]{40}$' -or $env:MRK_WINDOWS_SOURCE_TREE -cnotmatch '^[0-9a-f]{40}$' -or
                $env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_PRECHECK_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME -cne 'success') { throw 'binding' }
            $expectedDeps = [System.IO.Path]::Combine($expectedRoot, 'target', 'x86_64-pc-windows-msvc', 'debug', 'deps')
            if ([System.IO.Path]::GetDirectoryName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cne $expectedDeps -or
                [System.IO.Path]::GetFileName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cnotmatch '^mrk_windows_installed_native-[0-9a-f]{16}\.exe$') { throw 'artifact' }
            if ($env:MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME -cne 'success' -or
                $env:MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME -cne 'success') { throw 'prior-original-finality' }
            if ($env:MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME -cne 'success' -or
                $env:MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME -cne 'success') { throw 'prior-original-finality' }
            Set-Location -LiteralPath $expectedRoot
            # One fixed direct foreground original, never a Process object, launcher, pipe, retry or kill.
            & $env:MRK_WINDOWS_NATIVE_ARTIFACT qualification_fixture::hosted_observe_produced_version_before_collision --exact --ignored --nocapture --test-threads=1
            $originalExitCode = $LASTEXITCODE
            if ($originalExitCode -isnot [int]) { throw 'original-exit' }
            $command = '"' + $env:MRK_WINDOWS_NATIVE_ARTIFACT + '" qualification_fixture::hosted_observe_produced_version_before_collision --exact --ignored --nocapture --test-threads=1'
            $commandSha256 = [System.Convert]::ToHexString([System.Security.Cryptography.SHA256]::HashData([System.Text.Encoding]::Unicode.GetBytes($command))).ToLowerInvariant()
            $lines = @(
              'MRK_WINDOWS_RUNTIME_PUBLICATION_ORIGINAL_EXIT_V1'
              'profile=windows-runtime-publication-v1'
              "sourceSha=$($env:GITHUB_SHA)"
              "sourceTree=$($env:MRK_WINDOWS_SOURCE_TREE)"
              "runId=$($env:GITHUB_RUN_ID)"
              'attempt=1'
              'role=before'
              "artifactSha256=$($env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256)"
              "precheckSha256=$($env:MRK_WINDOWS_PRECHECK_SHA256)"
              "commandSha256=$commandSha256"
              'originalWaitReturned=true'
              "exitCode=$originalExitCode"
              'writerCloseGate=original-before-step-success-required'
            )
            $raw = [System.Text.UTF8Encoding]::new($false, $true).GetBytes(($lines -join "`n") + "`n")
            if ($raw.Length -eq 0 -or $raw.Length -gt 4096) { throw 'exit-bound' }
            $path = [System.IO.Path]::Combine($expectedRoot, 'producer-before-exit.private.txt')
            $stream = [System.IO.File]::Open($path, [System.IO.FileMode]::CreateNew, [System.IO.FileAccess]::Write, [System.IO.FileShare]::None)
            try { $stream.Write($raw, 0, $raw.Length); $stream.Flush() } finally { $stream.Dispose() }
            # Only THIS later original step success closes the preceding exit record.
            if ($originalExitCode -ne 0) { throw 'expected-original-exit' }
            exit 0
          } catch {
            [Console]::Error.WriteLine('MRK_WINDOWS_PRODUCER_REFUSED=before-original-or-exit-record')
            exit 1
          }
      - name: Validate separately closed before evidence as DATA
        id: producer-before-finalize
        if: success() && inputs.scope == 'windows-runtime-publication' && steps.producer-before.outcome == 'success'
        timeout-minutes: 2
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME: ${{ steps.producer-before.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py windows-runtime-publication-before-finalize'
      - name: Run the SAME no-argument helper and require typed settled occupied-target exit two
        id: producer-occupied
        if: success() && inputs.scope == 'windows-runtime-publication' && steps.producer-before-finalize.outcome == 'success'
        timeout-minutes: 4
        shell: pwsh -NoLogo -NoProfile -NonInteractive -Command ". '{0}'"
        env:
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_HELPER_ARTIFACT: ${{ steps.ordinary-preflight.outputs.helperArtifact }}
          MRK_WINDOWS_HELPER_ARTIFACT_SHA256: ${{ steps.ordinary-preflight.outputs.helperArtifactSha256 }}
          MRK_WINDOWS_SOURCE_TREE: ${{ steps.ordinary-preflight.outputs.sourceTree }}
          MRK_WINDOWS_PRECHECK_SHA256: ${{ steps.ordinary-preflight.outputs.precheckSha256 }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME: ${{ steps.producer-before.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-before-finalize.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $false
          try {
            $expectedRoot = [System.IO.Path]::Combine($env:RUNNER_TEMP, "mrk-windows-installed-native-$($env:GITHUB_RUN_ID)-1")
            if ($env:GITHUB_RUN_ATTEMPT -cne '1' -or $env:MRK_DESKTOP_CI_ROOT -cne $expectedRoot -or
                $env:GITHUB_EVENT_NAME -cne 'workflow_dispatch' -or $env:MRK_DESKTOP_DISPATCH_SCOPE -cne 'windows-runtime-publication' -or
                $env:GITHUB_REF -cne 'refs/heads/verify/desktop-windows-runtime-publication' -or
                $env:MRK_DESKTOP_EXPECTED_SHA -cne $env:GITHUB_SHA -or $env:GITHUB_WORKFLOW_SHA -cne $env:GITHUB_SHA -or
                $env:GITHUB_SHA -cnotmatch '^[0-9a-f]{40}$' -or $env:MRK_WINDOWS_SOURCE_TREE -cnotmatch '^[0-9a-f]{40}$' -or
                $env:MRK_WINDOWS_HELPER_ARTIFACT_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_PRECHECK_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME -cne 'success') { throw 'binding' }
            $expectedHelper = [System.IO.Path]::Combine($expectedRoot, 'mrk-windows-runtime-publish.exe')
            if ($env:MRK_WINDOWS_HELPER_ARTIFACT -cne $expectedHelper) { throw 'artifact' }
            if ($env:MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME -cne 'success' -or
                $env:MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME -cne 'success') { throw 'prior-original-finality' }
            if ($env:MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME -cne 'success' -or
                $env:MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME -cne 'success') { throw 'prior-original-finality' }
            if ($env:MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME -cne 'success' -or
                $env:MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME -cne 'success') { throw 'prior-original-finality' }
            Set-Location -LiteralPath $expectedRoot
            # One fixed direct foreground original, never a Process object, launcher, pipe, retry or kill.
            & $env:MRK_WINDOWS_HELPER_ARTIFACT
            $originalExitCode = $LASTEXITCODE
            if ($originalExitCode -isnot [int]) { throw 'original-exit' }
            $command = '"' + $env:MRK_WINDOWS_HELPER_ARTIFACT + '"'
            $commandSha256 = [System.Convert]::ToHexString([System.Security.Cryptography.SHA256]::HashData([System.Text.Encoding]::Unicode.GetBytes($command))).ToLowerInvariant()
            $lines = @(
              'MRK_WINDOWS_RUNTIME_PUBLICATION_ORIGINAL_EXIT_V1'
              'profile=windows-runtime-publication-v1'
              "sourceSha=$($env:GITHUB_SHA)"
              "sourceTree=$($env:MRK_WINDOWS_SOURCE_TREE)"
              "runId=$($env:GITHUB_RUN_ID)"
              'attempt=1'
              'role=helperOccupied'
              "artifactSha256=$($env:MRK_WINDOWS_HELPER_ARTIFACT_SHA256)"
              "precheckSha256=$($env:MRK_WINDOWS_PRECHECK_SHA256)"
              "commandSha256=$commandSha256"
              'originalWaitReturned=true'
              "exitCode=$originalExitCode"
              'writerCloseGate=original-helperOccupied-step-success-required'
            )
            $raw = [System.Text.UTF8Encoding]::new($false, $true).GetBytes(($lines -join "`n") + "`n")
            if ($raw.Length -eq 0 -or $raw.Length -gt 4096) { throw 'exit-bound' }
            $path = [System.IO.Path]::Combine($expectedRoot, 'producer-occupied-exit.private.txt')
            $stream = [System.IO.File]::Open($path, [System.IO.FileMode]::CreateNew, [System.IO.FileAccess]::Write, [System.IO.FileShare]::None)
            try { $stream.Write($raw, 0, $raw.Length); $stream.Flush() } finally { $stream.Dispose() }
            # Only THIS later original step success closes the preceding exit record.
            if ($originalExitCode -ne 2) { throw 'expected-original-exit' }
            exit 0
          } catch {
            [Console]::Error.WriteLine('MRK_WINDOWS_PRODUCER_REFUSED=helperOccupied-original-or-exit-record')
            exit 1
          }
      - name: Validate separately closed helperOccupied evidence as DATA
        id: producer-occupied-finalize
        if: success() && inputs.scope == 'windows-runtime-publication' && steps.producer-occupied.outcome == 'success'
        timeout-minutes: 2
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME: ${{ steps.producer-before.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-before-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_STEP_OUTCOME: ${{ steps.producer-occupied.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py windows-runtime-publication-occupied-finalize'
      - name: Independently compare every retained source and produced object after occupied refusal
        id: producer-after
        if: success() && inputs.scope == 'windows-runtime-publication' && steps.producer-occupied-finalize.outcome == 'success'
        timeout-minutes: 4
        shell: pwsh -NoLogo -NoProfile -NonInteractive -Command ". '{0}'"
        env:
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_NATIVE_ARTIFACT: ${{ steps.ordinary-preflight.outputs.artifact }}
          MRK_WINDOWS_NATIVE_ARTIFACT_SHA256: ${{ steps.ordinary-preflight.outputs.artifactSha256 }}
          MRK_WINDOWS_SOURCE_TREE: ${{ steps.ordinary-preflight.outputs.sourceTree }}
          MRK_WINDOWS_PRECHECK_SHA256: ${{ steps.ordinary-preflight.outputs.precheckSha256 }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME: ${{ steps.producer-before.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-before-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_STEP_OUTCOME: ${{ steps.producer-occupied.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_FINALIZE_STEP_OUTCOME: ${{ steps.producer-occupied-finalize.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $false
          try {
            $expectedRoot = [System.IO.Path]::Combine($env:RUNNER_TEMP, "mrk-windows-installed-native-$($env:GITHUB_RUN_ID)-1")
            if ($env:GITHUB_RUN_ATTEMPT -cne '1' -or $env:MRK_DESKTOP_CI_ROOT -cne $expectedRoot -or
                $env:GITHUB_EVENT_NAME -cne 'workflow_dispatch' -or $env:MRK_DESKTOP_DISPATCH_SCOPE -cne 'windows-runtime-publication' -or
                $env:GITHUB_REF -cne 'refs/heads/verify/desktop-windows-runtime-publication' -or
                $env:MRK_DESKTOP_EXPECTED_SHA -cne $env:GITHUB_SHA -or $env:GITHUB_WORKFLOW_SHA -cne $env:GITHUB_SHA -or
                $env:GITHUB_SHA -cnotmatch '^[0-9a-f]{40}$' -or $env:MRK_WINDOWS_SOURCE_TREE -cnotmatch '^[0-9a-f]{40}$' -or
                $env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_PRECHECK_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME -cne 'success') { throw 'binding' }
            $expectedDeps = [System.IO.Path]::Combine($expectedRoot, 'target', 'x86_64-pc-windows-msvc', 'debug', 'deps')
            if ([System.IO.Path]::GetDirectoryName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cne $expectedDeps -or
                [System.IO.Path]::GetFileName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cnotmatch '^mrk_windows_installed_native-[0-9a-f]{16}\.exe$') { throw 'artifact' }
            if ($env:MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME -cne 'success' -or
                $env:MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME -cne 'success') { throw 'prior-original-finality' }
            if ($env:MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME -cne 'success' -or
                $env:MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME -cne 'success') { throw 'prior-original-finality' }
            if ($env:MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME -cne 'success' -or
                $env:MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME -cne 'success') { throw 'prior-original-finality' }
            if ($env:MRK_WINDOWS_PRODUCER_OCCUPIED_STEP_OUTCOME -cne 'success' -or
                $env:MRK_WINDOWS_PRODUCER_OCCUPIED_FINALIZE_STEP_OUTCOME -cne 'success') { throw 'prior-original-finality' }
            Set-Location -LiteralPath $expectedRoot
            # One fixed direct foreground original, never a Process object, launcher, pipe, retry or kill.
            & $env:MRK_WINDOWS_NATIVE_ARTIFACT qualification_fixture::hosted_compare_after_occupied_producer --exact --ignored --nocapture --test-threads=1
            $originalExitCode = $LASTEXITCODE
            if ($originalExitCode -isnot [int]) { throw 'original-exit' }
            $command = '"' + $env:MRK_WINDOWS_NATIVE_ARTIFACT + '" qualification_fixture::hosted_compare_after_occupied_producer --exact --ignored --nocapture --test-threads=1'
            $commandSha256 = [System.Convert]::ToHexString([System.Security.Cryptography.SHA256]::HashData([System.Text.Encoding]::Unicode.GetBytes($command))).ToLowerInvariant()
            $lines = @(
              'MRK_WINDOWS_RUNTIME_PUBLICATION_ORIGINAL_EXIT_V1'
              'profile=windows-runtime-publication-v1'
              "sourceSha=$($env:GITHUB_SHA)"
              "sourceTree=$($env:MRK_WINDOWS_SOURCE_TREE)"
              "runId=$($env:GITHUB_RUN_ID)"
              'attempt=1'
              'role=after'
              "artifactSha256=$($env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256)"
              "precheckSha256=$($env:MRK_WINDOWS_PRECHECK_SHA256)"
              "commandSha256=$commandSha256"
              'originalWaitReturned=true'
              "exitCode=$originalExitCode"
              'writerCloseGate=original-after-step-success-required'
            )
            $raw = [System.Text.UTF8Encoding]::new($false, $true).GetBytes(($lines -join "`n") + "`n")
            if ($raw.Length -eq 0 -or $raw.Length -gt 4096) { throw 'exit-bound' }
            $path = [System.IO.Path]::Combine($expectedRoot, 'fullwalk-publisher-exit.private.json')
            $stream = [System.IO.File]::Open($path, [System.IO.FileMode]::CreateNew, [System.IO.FileAccess]::Write, [System.IO.FileShare]::None)
            try { $stream.Write($raw, 0, $raw.Length); $stream.Flush() } finally { $stream.Dispose() }
            # Only THIS later original step success closes the preceding exit record.
            if ($originalExitCode -ne 0) { throw 'expected-original-exit' }
            exit 0
          } catch {
            [Console]::Error.WriteLine('MRK_WINDOWS_PRODUCER_REFUSED=after-original-or-exit-record')
            exit 1
          }
      - name: Validate separately closed final publication evidence as DATA
        id: fixture-finalize
        if: success() && ((inputs.scope == 'windows-installed-fullwalk' && steps.fixture-publisher.outcome == 'success') || (inputs.scope == 'windows-runtime-publication' && steps.producer-after.outcome == 'success'))
        timeout-minutes: 2
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PUBLISHER_STEP_OUTCOME: ${{ inputs.scope == 'windows-runtime-publication' && steps.producer-after.outcome || steps.fixture-publisher.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME: ${{ steps.producer-before.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-before-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_STEP_OUTCOME: ${{ steps.producer-occupied.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_FINALIZE_STEP_OUTCOME: ${{ steps.producer-occupied-finalize.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py windows-installed-fixture-finalize'
      - name: Own one fixed ordinary native child by its original logon handles
        id: ordinary-owner
        if: success() && ((inputs.scope != 'windows-installed-fullwalk' && inputs.scope != 'windows-runtime-publication') || steps.fixture-finalize.outcome == 'success')
        timeout-minutes: 4
        shell: pwsh -NoLogo -NoProfile -NonInteractive -Command ". '{0}'"
        env:
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_NATIVE_ARTIFACT: ${{ steps.ordinary-preflight.outputs.artifact }}
          MRK_WINDOWS_NATIVE_ARTIFACT_SHA256: ${{ steps.ordinary-preflight.outputs.artifactSha256 }}
          MRK_WINDOWS_SOURCE_TREE: ${{ steps.ordinary-preflight.outputs.sourceTree }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PUBLISHER_STEP_OUTCOME: ${{ inputs.scope == 'windows-runtime-publication' && steps.producer-after.outcome || steps.fixture-publisher.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME: ${{ steps.producer-before.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-before-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_STEP_OUTCOME: ${{ steps.producer-occupied.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_FINALIZE_STEP_OUTCOME: ${{ steps.producer-occupied-finalize.outcome }}
          MRK_WINDOWS_FIXTURE_FINALIZE_STEP_OUTCOME: ${{ steps.fixture-finalize.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $false
          try {
            $expectedRoot = [System.IO.Path]::Combine($env:RUNNER_TEMP, "mrk-windows-installed-native-$($env:GITHUB_RUN_ID)-1")
            $expectedDeps = [System.IO.Path]::Combine($expectedRoot, 'target', 'x86_64-pc-windows-msvc', 'debug', 'deps')
            if ($env:GITHUB_RUN_ATTEMPT -cne '1' -or $env:MRK_DESKTOP_CI_ROOT -cne $expectedRoot -or
                [System.IO.Path]::GetDirectoryName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cne $expectedDeps -or
                [System.IO.Path]::GetFileName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cnotmatch '^mrk_windows_installed_native-[0-9a-f]{16}\.exe$' -or
                $env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_SOURCE_TREE -cnotmatch '^[0-9a-f]{40}$') { throw 'binding' }
            Set-Location -LiteralPath $expectedRoot
            # Direct foreground invocation. No Python timeout, managed Process
            # object, credential launcher, redirected pipe or detached borrower.
            & $env:MRK_WINDOWS_NATIVE_ARTIFACT ordinary_owner::hosted_ordinary_original_handle_contract --exact --ignored --nocapture --test-threads=1
            $originalExitCode = $LASTEXITCODE
            if ($originalExitCode -isnot [int]) { throw 'original-exit' }
            $receipt = [ordered]@{
              schemaVersion = 1; sourceSha = $env:GITHUB_SHA; sourceTree = $env:MRK_WINDOWS_SOURCE_TREE
              runId = $env:GITHUB_RUN_ID; attempt = 1; artifactSha256 = $env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256
              ownerTest = 'ordinary_owner::hosted_ordinary_original_handle_contract'
              originalWaitReturned = $true; exitCode = $originalExitCode
              writerCloseGate = 'original-owner-step-success-required'
            }
            $raw = [System.Text.UTF8Encoding]::new($false, $true).GetBytes(($receipt | ConvertTo-Json -Depth 4 -Compress))
            if ($raw.Length -eq 0 -or $raw.Length -gt 4096) { throw 'exit-bound' }
            $path = [System.IO.Path]::Combine($expectedRoot, 'ordinary-owner-exit.private.json')
            $stream = [System.IO.File]::Open($path, [System.IO.FileMode]::CreateNew, [System.IO.FileAccess]::Write, [System.IO.FileShare]::None)
            try { $stream.Write($raw, 0, $raw.Length); $stream.Flush() } finally { $stream.Dispose() }
            # The private writer cannot attest its own close from preceding
            # bytes. Finalize additionally requires THIS original step success.
            if ($originalExitCode -ne 0) { exit $originalExitCode }
          } catch {
            [Console]::Error.WriteLine('MRK_WINDOWS_ORDINARY_OWNER_REFUSED=original-owner-or-exit-record')
            exit 1
          }
      - name: Validate separately closed ordinary native and parent evidence as DATA
        id: ordinary-finalize
        if: success() && steps.ordinary-owner.outcome == 'success'
        timeout-minutes: 2
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_ORDINARY_OWNER_STEP_OUTCOME: ${{ steps.ordinary-owner.outcome }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PUBLISHER_STEP_OUTCOME: ${{ inputs.scope == 'windows-runtime-publication' && steps.producer-after.outcome || steps.fixture-publisher.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME: ${{ steps.producer-before.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-before-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_STEP_OUTCOME: ${{ steps.producer-occupied.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_FINALIZE_STEP_OUTCOME: ${{ steps.producer-occupied-finalize.outcome }}
          MRK_WINDOWS_FIXTURE_FINALIZE_STEP_OUTCOME: ${{ steps.fixture-finalize.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py windows-installed-native-finalize'
      - name: Bind original publication and ordinary finality before the fullwalk request
        id: fullwalk-preflight
        if: success() && (inputs.scope == 'windows-installed-fullwalk' || inputs.scope == 'windows-runtime-publication') && steps.ordinary-finalize.outcome == 'success' && steps.fixture-finalize.outcome == 'success'
        timeout-minutes: 2
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PUBLISHER_STEP_OUTCOME: ${{ inputs.scope == 'windows-runtime-publication' && steps.producer-after.outcome || steps.fixture-publisher.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME: ${{ steps.producer-before.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-before-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_STEP_OUTCOME: ${{ steps.producer-occupied.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_FINALIZE_STEP_OUTCOME: ${{ steps.producer-occupied-finalize.outcome }}
          MRK_WINDOWS_FIXTURE_FINALIZE_STEP_OUTCOME: ${{ steps.fixture-finalize.outcome }}
          MRK_WINDOWS_ORDINARY_OWNER_STEP_OUTCOME: ${{ steps.ordinary-owner.outcome }}
          MRK_WINDOWS_ORDINARY_FINALIZE_STEP_OUTCOME: ${{ steps.ordinary-finalize.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py windows-installed-fullwalk'
      - name: Own one protected-version headless app child by its original logon handles
        id: fullwalk-owner
        if: success() && (inputs.scope == 'windows-installed-fullwalk' || inputs.scope == 'windows-runtime-publication') && steps.fullwalk-preflight.outcome == 'success'
        timeout-minutes: 4
        shell: pwsh -NoLogo -NoProfile -NonInteractive -Command ". '{0}'"
        env:
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_NATIVE_ARTIFACT: ${{ steps.fullwalk-preflight.outputs.artifact }}
          MRK_WINDOWS_NATIVE_ARTIFACT_SHA256: ${{ steps.fullwalk-preflight.outputs.artifactSha256 }}
          MRK_WINDOWS_APP_ARTIFACT_SHA256: ${{ steps.fullwalk-preflight.outputs.appArtifactSha256 }}
          MRK_WINDOWS_REQUEST_SHA256: ${{ steps.fullwalk-preflight.outputs.requestSha256 }}
          MRK_WINDOWS_SOURCE_TREE: ${{ steps.fullwalk-preflight.outputs.sourceTree }}
          MRK_WINDOWS_FULLWALK_PREFLIGHT_STEP_OUTCOME: ${{ steps.fullwalk-preflight.outcome }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PUBLISHER_STEP_OUTCOME: ${{ inputs.scope == 'windows-runtime-publication' && steps.producer-after.outcome || steps.fixture-publisher.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME: ${{ steps.producer-before.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-before-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_STEP_OUTCOME: ${{ steps.producer-occupied.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_FINALIZE_STEP_OUTCOME: ${{ steps.producer-occupied-finalize.outcome }}
          MRK_WINDOWS_FIXTURE_FINALIZE_STEP_OUTCOME: ${{ steps.fixture-finalize.outcome }}
          MRK_WINDOWS_ORDINARY_OWNER_STEP_OUTCOME: ${{ steps.ordinary-owner.outcome }}
          MRK_WINDOWS_ORDINARY_FINALIZE_STEP_OUTCOME: ${{ steps.ordinary-finalize.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $false
          try {
            $expectedRoot = [System.IO.Path]::Combine($env:RUNNER_TEMP, "mrk-windows-installed-native-$($env:GITHUB_RUN_ID)-1")
            $expectedDeps = [System.IO.Path]::Combine($expectedRoot, 'target', 'x86_64-pc-windows-msvc', 'debug', 'deps')
            $profileAdmitted = ($env:MRK_DESKTOP_DISPATCH_SCOPE -ceq 'windows-installed-fullwalk' -and $env:GITHUB_REF -ceq 'refs/heads/verify/desktop-windows-installed-native') -or
                ($env:MRK_DESKTOP_DISPATCH_SCOPE -ceq 'windows-runtime-publication' -and $env:GITHUB_REF -ceq 'refs/heads/verify/desktop-windows-runtime-publication')
            if ($env:GITHUB_RUN_ATTEMPT -cne '1' -or $env:MRK_DESKTOP_CI_ROOT -cne $expectedRoot -or
                $env:GITHUB_EVENT_NAME -cne 'workflow_dispatch' -or -not $profileAdmitted -or
                $env:MRK_DESKTOP_EXPECTED_SHA -cne $env:GITHUB_SHA -or
                [System.IO.Path]::GetDirectoryName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cne $expectedDeps -or
                [System.IO.Path]::GetFileName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cnotmatch '^mrk_windows_installed_native-[0-9a-f]{16}\.exe$' -or
                $env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_SOURCE_TREE -cnotmatch '^[0-9a-f]{40}$') { throw 'binding' }
            if ($env:MRK_WINDOWS_APP_ARTIFACT_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_REQUEST_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_FULLWALK_PREFLIGHT_STEP_OUTCOME -cne 'success') { throw 'preflight' }
            Set-Location -LiteralPath $expectedRoot
            # One fixed direct foreground original. No Process object, pipe, retry or timeout-kill wrapper.
            & $env:MRK_WINDOWS_NATIVE_ARTIFACT ordinary_owner::hosted_protected_version_fullwalk_contract --exact --ignored --nocapture --test-threads=1
            $originalExitCode = $LASTEXITCODE
            if ($originalExitCode -isnot [int]) { throw 'original-exit' }
            $receipt = [ordered]@{
              schemaVersion = 1; sourceSha = $env:GITHUB_SHA; sourceTree = $env:MRK_WINDOWS_SOURCE_TREE
              runId = $env:GITHUB_RUN_ID; attempt = 1
              artifactSha256 = $env:MRK_WINDOWS_APP_ARTIFACT_SHA256
              ownerArtifactSha256 = $env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256; requestSha256 = $env:MRK_WINDOWS_REQUEST_SHA256
              ownerTest = 'ordinary_owner::hosted_protected_version_fullwalk_contract'
              originalWaitReturned = $true; exitCode = $originalExitCode
              writerCloseGate = 'original-owner-step-success-required'
            }
            $raw = [System.Text.UTF8Encoding]::new($false, $true).GetBytes(($receipt | ConvertTo-Json -Depth 4 -Compress))
            if ($raw.Length -eq 0 -or $raw.Length -gt 4096) { throw 'exit-bound' }
            $path = [System.IO.Path]::Combine($expectedRoot, 'fullwalk-owner-exit.private.json')
            $stream = [System.IO.File]::Open($path, [System.IO.FileMode]::CreateNew, [System.IO.FileAccess]::Write, [System.IO.FileShare]::None)
            try { $stream.Write($raw, 0, $raw.Length); $stream.Flush() } finally { $stream.Dispose() }
            # Only THIS later original step success closes the preceding exit record.
            if ($originalExitCode -ne 0) { exit $originalExitCode }
          } catch {
            [Console]::Error.WriteLine('MRK_WINDOWS_FULLWALK_OWNER_REFUSED=original-or-exit-record')
            exit 1
          }
      - name: Validate separately closed protected-version app and owner evidence as DATA
        id: fullwalk-finalize
        if: success() && (inputs.scope == 'windows-installed-fullwalk' || inputs.scope == 'windows-runtime-publication') && steps.fullwalk-owner.outcome == 'success'
        timeout-minutes: 2
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_FULLWALK_PREFLIGHT_STEP_OUTCOME: ${{ steps.fullwalk-preflight.outcome }}
          MRK_WINDOWS_FULLWALK_OWNER_STEP_OUTCOME: ${{ steps.fullwalk-owner.outcome }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PUBLISHER_STEP_OUTCOME: ${{ inputs.scope == 'windows-runtime-publication' && steps.producer-after.outcome || steps.fixture-publisher.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME: ${{ steps.producer-before.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-before-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_STEP_OUTCOME: ${{ steps.producer-occupied.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_FINALIZE_STEP_OUTCOME: ${{ steps.producer-occupied-finalize.outcome }}
          MRK_WINDOWS_FIXTURE_FINALIZE_STEP_OUTCOME: ${{ steps.fixture-finalize.outcome }}
          MRK_WINDOWS_ORDINARY_OWNER_STEP_OUTCOME: ${{ steps.ordinary-owner.outcome }}
          MRK_WINDOWS_ORDINARY_FINALIZE_STEP_OUTCOME: ${{ steps.ordinary-finalize.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py windows-installed-fullwalk-finalize'
      - name: Retire only exact recorded fixture originals after both successful finalizers
        id: fixture-retirement
        if: success() && inputs.scope == 'windows-installed-fullwalk' && steps.ordinary-finalize.outcome == 'success' && steps.fullwalk-finalize.outcome == 'success'
        timeout-minutes: 4
        shell: pwsh -NoLogo -NoProfile -NonInteractive -Command ". '{0}'"
        env:
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          MRK_WINDOWS_NATIVE_ARTIFACT: ${{ steps.fullwalk-preflight.outputs.artifact }}
          MRK_WINDOWS_NATIVE_ARTIFACT_SHA256: ${{ steps.fullwalk-preflight.outputs.artifactSha256 }}
          MRK_WINDOWS_REQUEST_SHA256: ${{ steps.fullwalk-preflight.outputs.requestSha256 }}
          MRK_WINDOWS_PUBLISHER_RECEIPT_SHA256: ${{ steps.fullwalk-preflight.outputs.publisherReceiptSha256 }}
          MRK_WINDOWS_SOURCE_TREE: ${{ steps.fullwalk-preflight.outputs.sourceTree }}
          MRK_WINDOWS_FULLWALK_PREFLIGHT_STEP_OUTCOME: ${{ steps.fullwalk-preflight.outcome }}
          MRK_WINDOWS_FULLWALK_OWNER_STEP_OUTCOME: ${{ steps.fullwalk-owner.outcome }}
          MRK_WINDOWS_FULLWALK_FINALIZE_STEP_OUTCOME: ${{ steps.fullwalk-finalize.outcome }}
          MRK_WINDOWS_PUBLISHER_STEP_OUTCOME: ${{ steps.fixture-publisher.outcome }}
          MRK_WINDOWS_FIXTURE_FINALIZE_STEP_OUTCOME: ${{ steps.fixture-finalize.outcome }}
          MRK_WINDOWS_ORDINARY_OWNER_STEP_OUTCOME: ${{ steps.ordinary-owner.outcome }}
          MRK_WINDOWS_ORDINARY_FINALIZE_STEP_OUTCOME: ${{ steps.ordinary-finalize.outcome }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $false
          try {
            $expectedRoot = [System.IO.Path]::Combine($env:RUNNER_TEMP, "mrk-windows-installed-native-$($env:GITHUB_RUN_ID)-1")
            $expectedDeps = [System.IO.Path]::Combine($expectedRoot, 'target', 'x86_64-pc-windows-msvc', 'debug', 'deps')
            if ($env:GITHUB_RUN_ATTEMPT -cne '1' -or $env:MRK_DESKTOP_CI_ROOT -cne $expectedRoot -or
                $env:GITHUB_EVENT_NAME -cne 'workflow_dispatch' -or $env:MRK_DESKTOP_DISPATCH_SCOPE -cne 'windows-installed-fullwalk' -or
                $env:MRK_DESKTOP_EXPECTED_SHA -cne $env:GITHUB_SHA -or
                [System.IO.Path]::GetDirectoryName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cne $expectedDeps -or
                [System.IO.Path]::GetFileName($env:MRK_WINDOWS_NATIVE_ARTIFACT) -cnotmatch '^mrk_windows_installed_native-[0-9a-f]{16}\.exe$' -or
                $env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_SOURCE_TREE -cnotmatch '^[0-9a-f]{40}$') { throw 'binding' }
            if ($env:MRK_WINDOWS_REQUEST_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_PUBLISHER_RECEIPT_SHA256 -cnotmatch '^[0-9a-f]{64}$' -or
                $env:MRK_WINDOWS_ORDINARY_FINALIZE_STEP_OUTCOME -cne 'success' -or
                $env:MRK_WINDOWS_FULLWALK_FINALIZE_STEP_OUTCOME -cne 'success') { throw 'finality' }
            Set-Location -LiteralPath $expectedRoot
            # One fixed direct foreground original. No Process object, pipe, retry or timeout-kill wrapper.
            & $env:MRK_WINDOWS_NATIVE_ARTIFACT qualification_fixture::hosted_retire_protected_version_fixture --exact --ignored --nocapture --test-threads=1
            $originalExitCode = $LASTEXITCODE
            if ($originalExitCode -isnot [int]) { throw 'original-exit' }
            $receipt = [ordered]@{
              schemaVersion = 1; sourceSha = $env:GITHUB_SHA; sourceTree = $env:MRK_WINDOWS_SOURCE_TREE
              runId = $env:GITHUB_RUN_ID; attempt = 1
              profile = 'windows-installed-fullwalk-v1'; artifactSha256 = $env:MRK_WINDOWS_NATIVE_ARTIFACT_SHA256
              requestSha256 = $env:MRK_WINDOWS_REQUEST_SHA256; publisherReceiptSha256 = $env:MRK_WINDOWS_PUBLISHER_RECEIPT_SHA256
              retirementTest = 'qualification_fixture::hosted_retire_protected_version_fixture'
              originalWaitReturned = $true; exitCode = $originalExitCode
              writerCloseGate = 'original-retirement-step-success-required'
            }
            $raw = [System.Text.UTF8Encoding]::new($false, $true).GetBytes(($receipt | ConvertTo-Json -Depth 4 -Compress))
            if ($raw.Length -eq 0 -or $raw.Length -gt 4096) { throw 'exit-bound' }
            $path = [System.IO.Path]::Combine($expectedRoot, 'fullwalk-retirement-exit.private.json')
            $stream = [System.IO.File]::Open($path, [System.IO.FileMode]::CreateNew, [System.IO.FileAccess]::Write, [System.IO.FileShare]::None)
            try { $stream.Write($raw, 0, $raw.Length); $stream.Flush() } finally { $stream.Dispose() }
            # Only THIS later original step success closes the preceding exit record.
            if ($originalExitCode -ne 0) { exit $originalExitCode }
          } catch {
            [Console]::Error.WriteLine('MRK_WINDOWS_RETIREMENT_REFUSED=original-or-exit-record')
            exit 1
          }
      - name: Observe actual PowerShell and loaded Process identity as DATA only
        id: runtime-data
        if: always() && steps.prepare.outcome == 'success'
        continue-on-error: true
        timeout-minutes: 1
        shell: pwsh -NoLogo -NoProfile -NonInteractive -Command ". '{0}'"
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          $ErrorActionPreference = 'Stop'
          $ProgressPreference = 'SilentlyContinue'
          try {
            $expectedRoot = [System.IO.Path]::Combine($env:RUNNER_TEMP, "mrk-windows-installed-native-$($env:GITHUB_RUN_ID)-1")
            if ($env:GITHUB_RUN_ATTEMPT -ne '1' -or $env:MRK_DESKTOP_CI_ROOT -cne $expectedRoot) { throw 'binding' }
            $assembly = [System.Diagnostics.Process].Assembly
            $name = $assembly.GetName()
            $attributes = $assembly.GetCustomAttributesData()
            if ($attributes.Count -gt 128) { throw 'metadata-bound' }
            $informational = $null
            $revision = $null
            foreach ($attribute in $attributes) {
              if ($attribute.AttributeType.FullName -eq 'System.Reflection.AssemblyInformationalVersionAttribute') {
                if ($null -ne $informational -or $attribute.ConstructorArguments.Count -ne 1) { throw 'metadata-shape' }
                $informational = [string]$attribute.ConstructorArguments[0].Value
              } elseif ($attribute.AttributeType.FullName -eq 'System.Reflection.AssemblyMetadataAttribute' -and
                        $attribute.ConstructorArguments.Count -eq 2 -and
                        [string]$attribute.ConstructorArguments[0].Value -in @('SourceRevisionId', 'RepositoryCommit', 'CommitHash')) {
                $value = [string]$attribute.ConstructorArguments[1].Value
                if ($null -ne $revision -and $revision -cne $value) { throw 'metadata-conflict' }
                $revision = $value
              }
            }
            $identity = [ordered]@{
              schemaVersion = 1; sourceSha = $env:GITHUB_SHA; runId = $env:GITHUB_RUN_ID; attempt = 1
              imageOS = $env:ImageOS; imageVersion = $env:ImageVersion
              powerShell = [ordered]@{ path = [Environment]::ProcessPath; version = $PSVersionTable.PSVersion.ToString(); edition = $PSVersionTable.PSEdition }
              runtime = [ordered]@{ version = [Environment]::Version.ToString(); framework = [System.Runtime.InteropServices.RuntimeInformation]::FrameworkDescription }
              processAssembly = [ordered]@{
                path = $assembly.Location; name = $name.Name; version = $name.Version.ToString()
                moduleVersionId = $assembly.ManifestModule.ModuleVersionId.ToString('D')
                informationalVersion = $informational; sourceRevision = $revision
              }
            }
            $raw = [System.Text.UTF8Encoding]::new($false, $true).GetBytes(($identity | ConvertTo-Json -Depth 6 -Compress))
            if ($raw.Length -eq 0 -or $raw.Length -gt 65536) { throw 'data-bound' }
            $path = [System.IO.Path]::Combine($expectedRoot, 'runtime-identity.private.json')
            $stream = [System.IO.File]::Open($path, [System.IO.FileMode]::CreateNew, [System.IO.FileAccess]::Write, [System.IO.FileShare]::None)
            try { $stream.Write($raw, 0, $raw.Length); $stream.Flush() } finally { $stream.Dispose() }
            # The actual observer remains alive during this fixed stable-file DATA handoff.
            & $env:MRK_PYTHON -I -S -B desktop/tools/ci_foundation.py windows-installed-runtime-data
            if ($LASTEXITCODE -ne 0) { throw 'data-validator' }
          } catch {
            [Console]::Error.WriteLine('MRK_WINDOWS_RUNTIME_DATA_REFUSED=observation')
            exit 1
          }
      - name: Retain only bounded original DATA without retry or cleanup
        id: retain
        if: always() && steps.prepare.outcome == 'success'
        shell: bash
        env:
          MRK_PYTHON: ${{ steps.python.outputs.python-path }}
          MRK_DESKTOP_CI_ROOT: ${{ steps.prepare.outputs.root }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
          MRK_WINDOWS_RUNTIME_DATA_STEP_OUTCOME: ${{ steps.runtime-data.outcome }}
          MRK_WINDOWS_ORDINARY_PREFLIGHT_STEP_OUTCOME: ${{ steps.ordinary-preflight.outcome }}
          MRK_WINDOWS_PUBLISHER_STEP_OUTCOME: ${{ inputs.scope == 'windows-runtime-publication' && steps.producer-after.outcome || steps.fixture-publisher.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_STEP_OUTCOME: ${{ steps.producer-stage.outcome }}
          MRK_WINDOWS_PRODUCER_STAGE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-stage-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_STEP_OUTCOME: ${{ steps.producer-success.outcome }}
          MRK_WINDOWS_PRODUCER_SUCCESS_FINALIZE_STEP_OUTCOME: ${{ steps.producer-success-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_STEP_OUTCOME: ${{ steps.producer-before.outcome }}
          MRK_WINDOWS_PRODUCER_BEFORE_FINALIZE_STEP_OUTCOME: ${{ steps.producer-before-finalize.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_STEP_OUTCOME: ${{ steps.producer-occupied.outcome }}
          MRK_WINDOWS_PRODUCER_OCCUPIED_FINALIZE_STEP_OUTCOME: ${{ steps.producer-occupied-finalize.outcome }}
          MRK_WINDOWS_FIXTURE_FINALIZE_STEP_OUTCOME: ${{ steps.fixture-finalize.outcome }}
          MRK_WINDOWS_ORDINARY_OWNER_STEP_OUTCOME: ${{ steps.ordinary-owner.outcome }}
          MRK_WINDOWS_ORDINARY_FINALIZE_STEP_OUTCOME: ${{ steps.ordinary-finalize.outcome }}
          MRK_WINDOWS_FULLWALK_PREFLIGHT_STEP_OUTCOME: ${{ steps.fullwalk-preflight.outcome }}
          MRK_WINDOWS_FULLWALK_OWNER_STEP_OUTCOME: ${{ steps.fullwalk-owner.outcome }}
          MRK_WINDOWS_FULLWALK_FINALIZE_STEP_OUTCOME: ${{ steps.fullwalk-finalize.outcome }}
          MRK_WINDOWS_RETIREMENT_STEP_OUTCOME: ${{ steps.fixture-retirement.outcome }}
        run: '"$MRK_PYTHON" -I -S -B desktop/tools/ci_foundation.py retain'
      - name: Upload bounded original verification evidence
        if: always() && steps.retain.outcome == 'success'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: desktop-windows-installed-native-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ steps.prepare.outputs.root }}/public/
          if-no-files-found: error
          retention-days: 14
          compression-level: 1
