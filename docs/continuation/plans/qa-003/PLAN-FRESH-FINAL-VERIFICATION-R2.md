# QA-003 — fresh complete frozen verification R2 after unexplained syscall failure

## Reanalysis and limits

The original QA-003 overlap finding, its R3 full-diff approval and the seven
approved helpers still apply to the exact171-file tree
`c6319b556696f89b8cea08fb7d95e1d5f09439df`. HEAD/main is
`2beb37336fa8002b69f598fe431082606368310d`; branch is
`fix/qa-003-local-signing-lease`; package0.3.0. All53 intended file hashes,
seven helper hashes, installed module bytes and user AGENTS remain unchanged.

The reviewed continuation was actually executed and FAILED, not interrupted.
`matrix-source-wheel-02` exit1 after339.062s is retained together with the
38-row ledger. Source completed1616 cases; wheel completed867 before the next
seed, active-build-pending/resolve, failed in activation with raw O_EXCL open of
`state.pending` raising EEXIST. The failing original namespace has committed
revision43 but no pending stage after unwind. Complete original observations are
in `FINAL-FAILURE-R1-OBSERVATION.json`. No original evidence has been rewritten.

The spontaneous syscall cause is **not established**. Do not label it a code
fix, a resolved root cause, an interruption, an environmental fact, or a pass.
The fixed namespace is private and held under a cooperating exclusive account
lease; inspected writers are synchronous. No reentrant writer or second toolkit
context was found. Evidence does not retain the suppressed secondary profile
checkpoint error or explain the later missing pending pathname.

Actual further probes against unchanged code:
- Exact failed seed/recovery256 repetitions PASS,36.968s.
- Complete installed-wheel shard02 with passive raw-open observations PASS:
  all1616 reached-cut results, identical expected/executed IDs and inventory
  hash to the original source run,232.923s. It is diagnostic, not a replacement
  for the final pair gate.
-20000 actual exclusive-create/write/fsync/replace/fsync/absence iterations
  PASS in a private disposable directory; no spontaneous collision reproduced.
- Distinct implementation reviewer exercised actual no-entry EEXIST, genuine
  existing-stage collision, create-then-error, unsafe-stage refusal and
  completed-build-result checkpoint failure. Original invocation fails, no
  subsequent native work is authorized, new admission refuses, and a separate
  recovery uses actual surviving authority; unsafe state remains preserved.
  These injections do not explain the spontaneous failure. Final distinct report:
  `implementation-review-final-failure-r1/INDEPENDENT-FAILURE-INVESTIGATION.md`,
  SHA256 `096b586419408aef92955074b87dc1c3b9f4befeeb272cc3429cd4ce2891f623`;
  its `REVIEW-EVIDENCE.json` SHA256
  `e25439f72136cf7ef299a487e715643519da6d6f966632e43ba87fffc8926ea1`.
  This report confirms the diagnostic gap and preserves the FAIL/no-waiver
  disposition; it does not approve this fresh invocation.

## Proposed action — no implementation changes, no failure waiver

Run the **entire original58-gate verification again from zero**, not a suffix
adapter and not a selective retry-until-green. Use a new sibling ignored evidence
namespace `.mobile-release/remediation/QA-003-final-r2/`. This avoids modifying,
moving or adopting any failed/original result, proof, partial fixture or wheel.
All16 source/wheel pairs and complete actual union must really run. No old pass,
diagnostic result, mock success or prior installation substitutes for a new gate.

There is no production correction to implement without an established defect.
The original QA-003 code remains under its distinct R3 implementation approval;
this proposal changes only the execution namespace/environment and verification
records. If renewed review or the fresh run identifies a code/fixture/helper
correction, stop, use the required plan/independent-review workflow and re-freeze.
Do not weaken O_EXCL, ownership, recovery, assertions, deadlines or coverage to
make the test pass. Do not repeat a failure silently or infer READY from a pass.

## Exact preparation and reviewed components

1. Revalidate R3 approval, frozen source/all seven helpers, empty real Git index,
   user AGENTS, original failed snapshot, wheel and absence of known completed
   diagnostic groups. Wait for the implementation reviewer to release every
   resource it used. No live Store or private signing state is accessed.
2. Exclusively create the new mode0700 sibling evidence directory. Copy **exact
   bytes/modes** of all seven approved helpers and `final-freeze.json` to it;
   compare each digest against the original freeze. The source root calculation
   `Path(__file__).parents[3]` and shell `git --show-toplevel` remain this checkout.
   The helpers derive every output from their own directory; none hardcodes the
   QA-003 basename. Do not edit helpers, transform AST, change commands, reuse
   results, or rebuild a different source tree. Bind copies, original approval,
   original failure and the new root/environment in an additional record.
3. Allocate a new mode0700 **task-owned temporary parent outside the checkout**
   under `ROOT.parent` with tempfile.mkdtemp. Persist its path/device/inode/mode
   and a unique canonical owner marker before execution. Give tests a new empty
   `tmp/` child and set TMPDIR to that exact path. This records and owns temporary
   resources without relying on the previously disappeared system-temp install.
   Record the actual new `tmp/` child's device/inode/uid/mode with its parent's
   identity before any gate; final cleanup must prove BOTH exact created objects.
   Do not claim the unknown earlier disappearance is thereby explained.
4. Before actual gates, distinct implementation reviewer compares all copied
   helpers/freeze and actual namespace/root calculation, reads the exact command
   invocation/environment/owner record and confirms there is no executable diff
   beyond path-independent approved copies. Original R3 and this approval both
   remain necessary. Run the copied `freeze.py --check` read-only too.
5. Invoke the byte-identical original `verify-all.py` with the same pinned Python
   and PATH/Ruby/Bundler tools, adding only the recorded TMPDIR. Its original
   environment,58 commands,900-second bounds, process cleanup, logging, fail-fast
   policy and results list stay unchanged. Persist separate log and real exit
   status in this new namespace; output must not preexist. Initial result list is
   empty. The wheel gate really rebuilds/installs from this exact frozen source;
   its wheel hash is newly recorded, not falsely claimed identical ZIP metadata.
6. Final acceptance requires all58 new rows PASS, unchanged original command
   inventory except expected evidence-root relocation, all actual source/wheel
   case proofs and complete union, package/test/resource equality, all log hashes,
   original freeze/AGENTS/helper equality, owned cleanup and complete new exit0.
   Retain the failed original invocation and state explicitly in closeout.

## Failure, retry, recovery and cleanup

A new failure stops the original runner. Preserve it and investigate; this plan
authorizes one fresh invocation, not automatic retries or acceptance of a later
partial shard. All unexecuted gates stay unexecuted. Any correction invalidates
this run's freeze and requires independent review and full validation again.
Controller loss never invents an exit code. Known child/group authority must be
reconciled without signaling stale journal PIDs or unrelated processes.

The old installation under `ROOT.parent/.mrk-qa003-final-matrix-tgf343b1` remains
**required forensic evidence** while the spontaneous failure's cause is unknown.
Keep its actual installed files, canonical owner marker, original wheel, logs,
failed namespace and freeze. No process remains merely to retain those files.
Fresh verification does not consume, adopt, overwrite, move or clean that old
installation. Byte-equal package files or reinstallability alone do not replace
an unresolved runtime/filesystem observation. Any later disposition needs its
own explicit evidence reconciliation; it is NOT authorized by this plan.
Do not invoke or bypass the old matrix cleanup gate, which correctly requires a
complete old union. All completed one-off reviewer installations and passing
probe case trees have already been removed; preserve required evidence rather
than satisfying disk-cleanup instructions by deleting something still needed.

The new original helpers clean their own smoke/matrix/build outputs. After final
worker completion inspect the new owned temporary parent. Remove only an empty
`tmp/` only after checking its recorded device/inode/uid/mode, unchanged owner marker and identical parent; if anything unexplained
remains, retain/report it and reconcile before cleanup. Never delete a shared
cache, another project/task, source/user files, required wheel or evidence. No
Gradle build/daemon is needed. No worker remains merely to wait between tasks.

## Security, compatibility and regression assessment

No change to public CLI behavior, credentials, Store state, evidence schemas,
production concurrency, signing policy, source/workflow identity, package runtime
dependencies, artifacts or supported platforms. Synthetic diagnostic files contain
no genuine signing material. No Store mutation/publication or private-keychain
inspection is authorized. Changing TMPDIR only selects a private supported local
volume for disposable verification data; record it and do not infer real consumer
credential/Xcode suitability from fixtures. Hosted Linux/macOS gates still must
run on the actual protected PR and main commit.

The root and reviewers must explicitly retain the unexplained original syscall
failure in final reconciliation/fresh audit. Passing this new run alone does not
establish its cause, justify a bug-free claim, or close the entire remediation.
QA-004, QA-005, MRK-008 and MRK-009 remain open; then the mandatory new complete
all-file/all-release-path audit and any new-blocker remediation precede READY and
the separate complete technical feature report. No progress count advances until
QA-003 has genuinely passed required checks and protected delivery.
