# Paused verifier: exact outstanding checklist

**Incomplete, unreviewed, no execution authority.**

The lists below are carried from the safely paused author checkpoint.
They are not new independent findings or completed changes.

## Six known draft concerns

1. native_reader failure tail accesses fixture._used/_finished without exact-type guard; lookalike inputs can mask the primary error. Reconcile statically.
2. Session gate reservation binds supplied cwd/environment but does not yet independently compare every gate with its original configuration.
3. Session _begin attempt/completion needs earlier original-ledger-exists and not-already-accepted checks before writes.
4. Nested source entry commitment reads current entry at dispatch; tie it to the originally frozen reference before accepting current bytes.
5. Collector/Lease cutoff, generic/no-channel behavior, control accounting, FD transfer and close-after-effect handling need complete actual-code review.
6. All remaining adoption/replay/finality assumptions need real-caller reconciliation; syntax success does not prove these mechanisms.

## Fourteen unfinished obligations

1. Complete static logic reconciliation of all draft classes/protocol/finality paths; current source is not accepted functional code.
2. Implement original fixture request, source-only minimal creation, genuine pinned pip24/setuptools79.0.1 seed, independently expected1052 files/142 dirs and compact observations.
3. Implement strict produced/pre-existing bootstrap union and original-root downgrade prevention at every consumer.
4. Implement genuine same-base editable successor: exact12 files/four dirs, independently derived payload/RECORD/metadata, empty editable source projection and historical/current distinction.
5. Wire original Proofs factory/profile/scopes/case/source stamp before request/freeze; explicit case propagation through all methods and9 copied drivers.
6. Move worker-consumer source creation before freeze; reorder generated child admissions before application/backend metadata/import.
7. Integrate verify-all actual split dispatch reservations, one unreset editable action, retained actual rows and successful-return observations, preserving every original profile/action.
8. Integrate process_gate.execute/numeric query authority, inherited cutoffs and control accounting without new budgets or changed generic45-gate route.
9. Implement earliest admission throughout bindings, owned_outputs, frozen_diff and all real helper entry roles.
10. Convert every produced mutable-ledger consumer and forbid final-verification immutable backing references in produced FrozenAuthority.
11. Implement distinct non-upgradable EvidenceInspection/shared strict parsers; preserve all69 deep schema assertions separately from early capability rejection.
12. Update strict request/attempt/completion/control accounting schemas and complete source/freezer bindings.
13. Add every required original/adversarial/after-effect/FD/failure-injection/genuine-positive regression source; none added or run for this amendment.
14. Freeze full actual before/diff/final package; obtain distinct independent implementation review and separate finite execution authorization before helper imports/execution.

## Original proof scope and limits

103+62 pure;219 original;30 mapped;5 integration;143 VPG (69 schema/67 preparation);25 historical mappings;10 controller;24 protocol;45 real gates/order;13 Ruby files;two separate Bash syntax commands

action120s/native30s/active-native1/original outer900;4GiB floor/512MiB headroom;all original byte/name/JSON/graph/tool/inventory/count limits unchanged;no supervisor/worker/handshake action or quota refresh

## Separate unresolved compatibility requirement

Generic build-isolated setuptools80.9.0 versus fixture79.0.1 remains unresolved, not waived or newly confirmed here.
