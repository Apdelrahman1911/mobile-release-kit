source "https://rubygems.org"

ruby "3.3.12"

# Store mutations are deliberately kept behind this exact, reviewed version.
gem "fastlane", "= 2.235.0"
# Match the reviewed default Fiddle used by isolated, gem-disabled helpers.
gem "fiddle", "= 1.1.2", require: false
gem "google-apis-androidpublisher_v3", "= 0.106.0"
gem "multi_json", ">= 1.15", "< 2.0"

group :test do
  gem "minitest", "= 5.25.5", require: false
end
